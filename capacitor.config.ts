import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.hindsight.app',
  appName: 'HindSight-frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
 